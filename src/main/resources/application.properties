spring.application.name=gym-planner

# H2 Database Configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# DataSource Properties - PostgreSQL
# Costruiamo l'URL dalle sue parti, che Render ci fornirà
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_DATABASE}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# DataSource Properties - H2
#spring.datasource.url=jdbc:h2:file:./data/gymplannerdb
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.hibernate.ddl-auto=update

# JPA Properties (Utili per vedere le query SQL generate)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

